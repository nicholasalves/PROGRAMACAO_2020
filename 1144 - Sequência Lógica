#include <stdio.h>
 
int main() {
    int x,y,z,j,k,w;
    scanf("%d",&x);
    for(y=1;y<=x;y++)
    {
      z=y*y;
      j=y*y*y;
      printf("%d %d %d\n",y,z,j);
      k=z+1;
      w=j+1;
       printf("%d %d %d\n",y,k,w);
    }
    return 0;
}
